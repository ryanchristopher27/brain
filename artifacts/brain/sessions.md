# brain — session summaries

_Extracted from brain's `updates/queue.md` at 2026-08-31T03:07:43Z._

## 2026-06-06 — brain (frontend domain)

- Consolidated two external suites (ui-ux-pro-max MIT, impeccable Apache-2.0) into `domains/frontend/` — brain's first populated L2 domain: vendored stdlib-Python search + Node anti-pattern detector, 9 synthesized references, priority-ladder rules.md, `/design` command, Cursor rule, detection. All 8 milestones built, reviewed, verified end-to-end.
- Patterns: (1) Re-check conditionally-deferred review findings at the milestone that could trigger them — deferring S1 "until --persist is wired" then re-checking in the final review caught a live path-traversal; (2) Verify external source-of-truth (actual file tree) before planning around it — impeccable's README advertised 7 reference files that don't exist, forcing a mid-build pivot to synthesis; (3) Vendoring third-party code transfers its security surface to you — budget a security pass per vendored component; (4) Plan risk-table items got handled at the right milestones, the one unforeseen risk (source reality) caused the only pivot.
- Brain improvements: (1) RECURRING (3rd session) — the plan→build/scaffold transition wants an explicit "stress-test the plan against reality" beat; this session's variant is that /plan should inspect external/vendored source structure before planning around its README; (2) A reusable "vendoring checklist" (license+NOTICE, dependency audit, security surface, runtime path resolution) would have structured the M2/M3/M8 work that was re-derived ad hoc.

## 2026-07-21 — brain (agent fleet + voice)

- Planned + scaffolded a 3-pillar initiative: agent fleet (`agents/` resource type + personas + lean MCPs), a local voice core (`voice/`, headless `claude -p` bridge, pluggable local-first STT/TTS), and a voice visualizer (`web/`). 30 runnable-stub files; plan appended to docs/plan.md.
- Patterns: (1) "verify reality before planning" generalizes from vendored source to *host tooling* — checked install.sh sync logic + CLI flag surface + hardware before drafting, so the plan named the real gap (install.sh has no agents concept) instead of assuming; (2) persona-as-permission-profile — bind safety to which subagent you summon (frontmatter tool allowlist) rather than a global posture; (3) when a request smells like reinventing a solved thing (a "chat webpage"), name the trap and offer the version that fills the unmet need (live voice visualizer); (4) one-backend-two-frontends: emit events once, let audio + web both consume, to avoid a second integration.
- Brain improvements: (1) `agents/` is a new first-class resource type but `install.sh` and the `post-edit-install-sync.sh` hook don't sync it yet — brain should treat "add a resource type" as a checklist (dir + template + install-sync + hook + CLAUDE.md doc); (2) /scaffold correctly deferred behavior-bearing files (install.sh/CLAUDE.md) to build — that boundary worked well and is worth encoding as explicit /scaffold guidance.

## 2026-07-23 — brain (agent fleet + voice: A1/A3/B1–B4/C1)

- Built the fleet + full voice loop: agents/ resource type + install.sh sync + 4 personas (A1), orchestration rule + /fleet (A3), voice core hotkey→whisper(Metal)→headless claude -p→say with a live web visualizer (B1–B4, C1). Working local voice assistant wired to the agent fleet, mic-free-testable via --check/--ask/--demo.
- Patterns: (1) HARDWARE-FREE VERIFICATION as a first-class deliverable — when a feature needs a mic/permissions/browser/voice you can't drive, build text-driven + stubbed + synthetic-input test modes as part of the milestone (say+afconvert to test STT with no mic; stubbed ws client for the event contract; --check/--ask/--demo; dry-run install into a temp dir). Every milestone stayed genuinely verified. Worth a brain rule. (2) verify-reality-first extended to probing a CLI's real flag surface + output shape before wiring a subprocess (caught --agent singular, stream-json shape, and empirically confirmed persona tool-allowlist enforcement headless). (3) dry-run before touching global state caught a README-installed-as-agent bug pre-global. (4) verify deps inside the actual venv, not the base interpreter.
- Brain improvements: (1) post-edit-install-sync.sh re-runs the FULL install.sh on every brain edit — it merged personal MCPs globally before tokens existed, re-created a bad artifact before a guard, and spams .bak files; make it scoped (sync only the changed resource type, skip identical files, don't merge personal MCPs missing their env vars). (2) brain has no concept of "runtime modules" (voice/, web/ with their own venv/deps) vs static resource configs — install.sh doesn't manage venvs; a convention would help. (3) RECURRING (2nd time) — "add a resource type" wants a checklist (dir+template+install-sync+hook+CLAUDE.md doc), re-derived ad hoc again in A1. (4) /build or /verify could prompt "is there a hardware-free way to verify this?" as a default beat.

## 2026-07-26 — brain (agent dashboard D1–D9 + polish)

- Shipped the full local agent dashboard (Pillar C v2): observe (server+security, orb tile, roster/jobs/health, work-pipeline board) + control (SQLite run registry, async process manager, run-level write approval w/ --add-dir confinement, controls+activity+cost panel, task queue). /design polish pass on web/ (detector clean). Used /fleet for parallel read-only recon (Scout design + Reviewer threat-model), sequential build.
- Patterns: (1) /fleet is for parallel RECON on shared-surface sequential builds, NOT a Builder swarm — the recon (design + threat-model + CLI probes) de-risked the whole phase; (2) probe security mechanisms empirically — `--allowed-tools` does NOT confine a persona (frontmatter wins; not a sandbox), `--add-dir` DOES confine (escape denied under acceptEdits), `--permission-prompt-tool` doesn't exist → forced the D8 run-level-approval pivot; (3) "suspect the check first" — several iterations lost to harness bugs on simple paths (macOS no `timeout`; tokenless health call; TestClient `Host: testserver`); (4) per-file `node --check` misses cross-script global `const` collisions — use combined-scope `cat a.js b.js | node --check`; (5) Reviewer threat-model → hardened endpoint contract built straight into the action API (persona allowlist, task-as-argv, confinement, token, audit, kill-group).
- Brain improvements: (1) the combined-scope JS check + "verify the verifier" heuristic are reusable — worth folding into the frontend domain's audit guidance and/or the hardware-free-verification memory; (2) brain projects that run a dev server hit real restart friction (manual kill+nohup, reaped backgrounds, stale ports) — a documented `uvicorn --reload`/dev-run pattern would help; (3) /design polish + the detector proved a strong closing quality gate — consider making a polish pass a default pre-commit beat for UI work.

## 2026-07-26 — brain (task tracker v1: T1–T5)

- Built a fleet-driven, cross-device task tracker: git-versioned .brain/tasks/*.md per repo + tracker.py (T1), task/project API (T2), Kanban board (T3), work-a-task→scoped approved fleet run linked back (T4), auto-Reviewer on write work→review (T5). Closes brain's own loop (plan→tasks→fleet→review). Verified incl. a real Builder→Reviewer auto-review run.
- Patterns: (1) reuse-first framing collapses scope — "a task is a persistent, stateful fleet run" made T4/T5 tiny (tag a run + finish hook, reusing registry/process-manager/D8/D9); (2) TDD scaffold — scaffold the acceptance criteria as runnable RED tests, build turns them green (the idempotent-serialize test guaranteed clean git diffs by construction); (3) surface + revise a LOCKED decision when a new hard requirement lands (cross-device → pivoted SQLite-in-home to git-versioned files, which was more brain-native); (4) a finish-hook that can spawn more work needs a re-entrancy guard (Reviewer run tagged review=True or it recurses forever); (5) dogfooding as verification — brain tracked its own tasks through the real lifecycle.
- Brain improvements: (1) RECURRING (2nd arc) — dev-server restart friction (manual kill+nohup on every backend edit); brain projects with a dev server want a documented `uvicorn --reload` dev-run pattern (candidate for the /run skill or a project convention). (2) The brainstorm→plan→scaffold→build→reflect cadence at PER-MILESTONE grain worked extremely well across this whole session — decision-locked, no mid-build ambiguity; validated. (3) TDD-scaffold (acceptance-tests-as-red-scaffold) is worth offering as a /scaffold option for testable modules. (4) real-agent verification cost is real; a cheap "dry-run/echo" persona or a mock mode for testing fleet plumbing would cut cost.

## 2026-07-26 — brain (workflow ↔ tracker integration W1–W5)

- Made the task tracker self-maintaining via the workflow: tracker.py `source`+idempotent `upsert` + standalone `dashboard/tracker_cli.py` (W1); Task-Sync hooks added to /plan (milestones→tasks), /build (doing→review + run link), /review (findings→bug/issue), /reflect (close done + file next-steps) command prompts + specs (W2–W5). Seeded brain's open backlog (11 tasks); fully dogfooded (W-series tracked→done via the hooks).
- Patterns: (1) prompt-driven sync beats parsing — commands hold their output in context, so they call the CLI directly (no markdown-table parsing); (2) idempotent `source` keys (plan:/review:/reflect:) make re-running a phase update-in-place, never duplicate; (3) dogfooding end-to-end validated the feature (brain tracked building its own tracker); (4) verify the INVOCATION mechanism not just the unit — the CLI passed tests but needed sys.path handling to run from another project's cwd (relative imports); caught before wiring.
- Brain improvements: (1) DOCUMENT the command-reload nuance in CLAUDE.md — editing an existing command's .md takes effect on the NEXT INVOCATION (content re-read), but adding a NEW command needs a session restart to register. I mis-stated this ("next session") repeatedly; it explains why /fleet needed a restart but the plan/build/review/reflect hooks went live immediately. (2) the workflow-hook pattern is validated and could extend to /brainstorm, /scaffold, /iterate. (3) minor: backlog seeding skipped the then-in-progress W1 (cosmetic).
