# Copy to .env and fill in ONLY if you enable a cloud backend in config.toml.
# Local defaults (whisper + say) need none of these. Never commit .env.

# STT (cloud, optional)
DEEPGRAM_API_KEY=

# TTS (cloud, optional)
ELEVENLABS_API_KEY=
OPENAI_API_KEY=
